spring.application.name=budgetgo-backend
server.port=4001
server.forward-headers-strategy=native

# --- AWS RDS MySQL Database ---
spring.datasource.url=jdbc:mysql://budgetgo-db.cm7o2q8wi8yf.us-east-1.rds.amazonaws.com:3306/budgetgo
spring.datasource.username=admin
spring.datasource.password=Karneyal123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --- JPA / Hibernate ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# Disable SQL init by default
spring.sql.init.mode=never

# Frontend URL for invitation links
app.frontend.url=https://main.d30y176duyuf0p.amplifyapp.com

# --- Razorpay Configuration ---
razorpay.key.id=rzp_test_YOUR_KEY_HERE
razorpay.key.secret=YOUR_SECRET_HERE

# --- Google OAuth2 Configuration ---
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID:YOUR_GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET:YOUR_GOOGLE_CLIENT_SECRET}
spring.security.oauth2.client.registration.google.client-authentication-method=client_secret_post
spring.security.oauth2.client.registration.google.scope=email,profile
spring.security.oauth2.client.registration.google.redirect-uri=https://hvr7ssnubd.us-east-1.awsapprunner.com/login/oauth2/code/google
spring.security.oauth2.client.provider.google.authorization-uri=https://accounts.google.com/o/oauth2/v2/auth
spring.security.oauth2.client.provider.google.token-uri=https://oauth2.googleapis.com/token
spring.security.oauth2.client.provider.google.user-info-uri=https://www.googleapis.com/oauth2/v3/userinfo
spring.security.oauth2.client.provider.google.user-name-attribute=sub

# AI Configuration
gemini.api.key=${GEMINI_API_KEY:YOUR_GEMINI_API_KEY}


# --- Email Configuration (Gmail) ---
# NOTE: Gmail SMTP often requires an App Password or OAuth2. If email fails to send,
# set `app.email.mock=true` (default) and use the /api/dev/emails/latest endpoint to
# view the generated OTP / invitation link.
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=budgetgo3@gmail.com
spring.mail.password=mqvepowukexglivw
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Use mock email delivery in dev (store last email in memory and do not send)
app.email.mock=false

